---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Guía Básica de Google Colab">
    <div class="main-wrapper">
        <div class="background-glow"></div>

        <div class="container">
            <header class="header animate-fade-in">
                <h1 class="title">
                    Guía de Inicio <br />
                    <span class="text-gradient">Google Colab</span>
                </h1>
                <p class="subtitle">
                    Tu entorno Jupyter Notebook gratuito en la nube. Ejecuta
                    Python sin configuraciones.
                </p>

                <div class="actions">
                    <a
                        class="button-primary"
                        href="https://colab.research.google.com"
                        target="_blank"
                    >
                        Abrir Google Colab
                    </a>
                </div>
            </header>

            <section class="grid">
                <div
                    class="card intro-card animate-slide-up"
                    style="--delay: 100ms"
                >
                    <div class="card-icon"></div>
                    <h3>¿Qué es Google Colab?</h3>
                    <p>
                        Google Colaboratory es un entorno de Jupyter Notebook
                        gratuito que se ejecuta completamente en la nube. Te
                        permite escribir y ejecutar código Python, ideal para
                        Data Science y Machine Learning.
                    </p>
                    <div class="features-list">
                        <span class="feature-tag">GPUs Gratuitas</span>
                        <span class="feature-tag">Comparte fácil</span>
                        <span class="feature-tag">Integra Drive</span>
                    </div>
                </div>

                <div class="card animate-slide-up" style="--delay: 200ms">
                    <div class="card-header">
                        <h3>Atajos de Teclado</h3>
                    </div>
                    <div class="shortcuts">
                        <div class="shortcut-item">
                            <span>Ejecutar celda</span>
                            <div class="keys">
                                <kbd>Ctrl</kbd> + <kbd>Enter</kbd>
                            </div>
                        </div>
                        <div class="shortcut-item">
                            <span>Ejecutar y avanzar</span>
                            <div class="keys">
                                <kbd>Shift</kbd> + <kbd>Enter</kbd>
                            </div>
                        </div>
                        <div class="shortcut-item">
                            <span>Nueva celda arriba</span>
                            <div class="keys"><kbd>Ctrl</kbd> M A</div>
                        </div>
                        <div class="shortcut-item">
                            <span>Nueva celda abajo</span>
                            <div class="keys"><kbd>Ctrl</kbd> M B</div>
                        </div>
                    </div>
                </div>

                <div class="card animate-slide-up" style="--delay: 300ms">
                    <h3>Instalación de Librerías</h3>
                    <p class="text-sm">
                        Usa el signo <code>!</code> para comandos de shell.
                    </p>

                    <div class="terminal-window">
                        <div class="terminal-header">
                            <span class="dot red"></span>
                            <span class="dot yellow"></span>
                            <span class="dot green"></span>
                        </div>
                        <div class="code-content">
                            <span class="code-comment"
                                ># Instalar paquetes pip</span
                            ><br />
                            <span class="code-keyword">!</span>pip install <span
                                class="code-string">pandas numpy</span
                            ><br /><br />
                            <span class="code-comment"
                                ># Desde requirements.txt</span
                            ><br />
                            <span class="code-keyword">!</span>pip install <span
                                class="code-string">-r requirements.txt</span
                            >
                        </div>
                    </div>
                </div>

                <div class="card animate-slide-up" style="--delay: 400ms">
                    <h3>Gestión de Archivos</h3>
                    <div class="terminal-window">
                        <div class="terminal-header">
                            <span class="dot red"></span>
                            <span class="dot yellow"></span>
                            <span class="dot green"></span>
                        </div>
                        <div class="code-content">
                            <span class="code-comment"
                                ># 1. Montar Google Drive</span
                            ><br />
                            <span class="code-keyword">from</span> google.colab <span
                                class="code-keyword">import</span
                            > drive<br />
                            drive.mount(<span class="code-string"
                                >'/content/drive'</span
                            >)<br /><br />

                            <span class="code-comment"
                                ># 2. Subir archivos locales</span
                            ><br />
                            <span class="code-keyword">from</span> google.colab <span
                                class="code-keyword">import</span
                            > files<br />
                            uploaded = files.upload()
                        </div>
                    </div>
                </div>

                <div class="card animate-slide-up" style="--delay: 500ms">
                    <h3>Aceleración de Hardware</h3>
                    <div class="info-box">
                        <p>
                            Ve a <strong>Entorno de ejecución</strong> → <strong
                                >Cambiar tipo de entorno</strong
                            > → Selecciona <strong>T4 GPU</strong>.
                        </p>
                    </div>
                    <div class="terminal-window">
                        <div class="code-content">
                            <span class="code-comment"
                                ># Verificar GPU asignada</span
                            ><br />
                            <span class="code-keyword">!</span>nvidia-smi
                        </div>
                    </div>
                </div>

                <div class="card animate-slide-up" style="--delay: 200ms">
                    <div class="card-header">
                        <h3>Limitaciones Free Tier</h3>
                    </div>
                    <div class="shortcuts">
                        <div class="shortcut-item">
                            <span
                                >Reinicio de entorno cada 12 horas (aprox).</span
                            >
                        </div>
                        <div class="shortcut-item">
                            <span>
                                Los archivos temporales se borran al cerrar.
                            </span>
                        </div>
                        <div class="shortcut-item">
                            <span>Disponibilidad de GPU variable.</span>
                        </div>
                    </div>
                </div>

                <div
                    class="card full-width animate-slide-up"
                    style="--delay: 600ms"
                >
                    <h3>Ejemplo: Data Science Básico</h3>
                    <div class="split-content">
                        <div class="text-content">
                            <p>
                                Un flujo de trabajo típico incluye importar
                                pandas, leer un CSV y graficar.
                            </p>
                            <ul class="check-list">
                                <li>Importar librerías</li>
                                <li>Cargar Dataset</li>
                                <li>Visualizar resultados</li>
                            </ul>
                        </div>
                        <div class="terminal-window">
                            <div class="terminal-header">
                                <span class="dot red"></span>
                                <span class="dot yellow"></span>
                                <span class="dot green"></span>
                                <span class="filename">analysis.py</span>
                            </div>
                            <div class="code-content">
                                <span class="code-keyword">import</span> pandas <span
                                    class="code-keyword">as</span
                                > pd<br />
                                <span class="code-keyword">import</span> matplotlib.pyplot
                                <span class="code-keyword">as</span> plt<br
                                /><br />

                                <span class="code-comment"># Cargar datos</span
                                ><br />
                                df = pd.read_csv(<span class="code-string"
                                    >'data.csv'</span
                                >)<br /><br />

                                <span class="code-comment"
                                    ># Crear gráfico simple</span
                                ><br />
                                plt.figure(figsize=(10,6))<br />
                                plt.plot(df[<span class="code-string"
                                    >'ventas'</span
                                >])<br />
                                plt.title(<span class="code-string"
                                    >'Ventas Mensuales'</span
                                >)<br />
                                plt.show()
                            </div>
                        </div>
                    </div>
                </div>
            </section>
        </div>
    </div>
</Layout>

<style>
    /* 1. Definición de Variables y Reset */
    :root {
        --bg-dark: #09090b;
        --bg-card: #18181b;
        --bg-card-hover: #27272a;
        --border-color: rgba(255, 255, 255, 0.1);
        --text-main: #f4f4f5;
        --text-muted: #a1a1aa;
        --gradient-title: linear-gradient(135deg, #f59e0b 0%, #ea580c 100%);
    }

    /* 2. Estructura General */
    .main-wrapper {
        background-color: var(--bg-dark);
        min-height: 100vh;
        color: var(--text-main);
        font-family: "Inter", system-ui, sans-serif;
        position: relative;
        overflow-x: hidden;
    }

    .background-glow {
        position: absolute;
        top: -20%;
        left: 50%;
        transform: translateX(-50%);
        width: 60vw;
        height: 60vh;
        background: radial-gradient(
            circle,
            rgba(245, 158, 11, 0.15) 0%,
            transparent 70%
        );
        pointer-events: none;
        z-index: 0;
    }

    .container {
        max-width: 1100px;
        margin: 0 auto;
        padding: 60px 20px;
        position: relative;
        z-index: 1;
    }

    /* 3. Header */
    .header {
        text-align: center;
        margin-bottom: 80px;
    }

    .badge {
        display: inline-block;
        padding: 6px 12px;
        border-radius: 20px;
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid var(--border-color);
        font-size: 0.85rem;
        margin-bottom: 20px;
        color: var(--accent-orange);
    }

    .title {
        font-size: 3.5rem;
        line-height: 1.1;
        font-weight: 800;
        margin-bottom: 16px;
        letter-spacing: -0.02em;
    }

    .text-gradient {
        background: var(--gradient-title);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .subtitle {
        color: var(--text-muted);
        font-size: 1.2rem;
        max-width: 600px;
        margin: 0 auto 32px auto;
    }

    .button-primary {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        background: var(--text-main);
        color: var(--bg-dark);
        padding: 12px 24px;
        border-radius: 8px;
        font-weight: 600;
        text-decoration: none;
        transition: transform 0.2s;
    }

    .button-primary:hover {
        transform: scale(1.05);
    }

    /* 4. Grid y Cards */
    .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
        gap: 24px;
        margin-bottom: 40px;
    }

    .card {
        background: var(--bg-card);
        border: 1px solid var(--border-color);
        border-radius: 16px;
        padding: 24px;
        transition: all 0.3s ease;
        display: flex;
        flex-direction: column;
    }

    .card:hover {
        border-color: var(--accent-orange);
        transform: translateY(-4px);
        box-shadow: 0 10px 30px -10px rgba(0, 0, 0, 0.5);
    }

    .card.full-width {
        grid-column: 1 / -1;
    }

    .card h3 {
        font-size: 1.25rem;
        margin-bottom: 16px;
        color: var(--text-main);
        display: flex;
        align-items: center;
        gap: 8px;
    }

    .intro-card p {
        color: var(--text-muted);
        line-height: 1.6;
        margin-bottom: 20px;
    }

    .features-list {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
    }

    .feature-tag {
        font-size: 0.8rem;
        padding: 4px 10px;
        background: rgba(255, 255, 255, 0.05);
        border-radius: 6px;
        color: var(--text-muted);
    }

    /* 5. Shortcuts Styling */
    .shortcuts {
        display: flex;
        flex-direction: column;
        gap: 12px;
    }

    .shortcut-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-size: 0.9rem;
        color: var(--text-muted);
        padding-bottom: 8px;
        border-bottom: 1px solid var(--border-color);
    }

    .shortcut-item:last-child {
        border-bottom: none;
    }

    kbd {
        background: rgba(255, 255, 255, 0.1);
        padding: 2px 6px;
        border-radius: 4px;
        font-family: monospace;
        font-size: 0.8rem;
        color: white;
        border-bottom: 2px solid rgba(255, 255, 255, 0.1);
    }

    /* 6. Terminal Window Styling */
    .terminal-window {
        background: #0f1115; /* Más oscuro que la card */
        border-radius: 8px;
        border: 1px solid var(--border-color);
        overflow: hidden;
        margin-top: auto; /* Empuja al fondo de la card */
    }

    .terminal-header {
        background: rgba(255, 255, 255, 0.03);
        padding: 8px 12px;
        display: flex;
        align-items: center;
        gap: 6px;
        border-bottom: 1px solid var(--border-color);
    }

    .dot {
        width: 10px;
        height: 10px;
        border-radius: 50%;
    }
    .dot.red {
        background: #ef4444;
    }
    .dot.yellow {
        background: #f59e0b;
    }
    .dot.green {
        background: #22c55e;
    }

    .filename {
        margin-left: auto;
        font-size: 0.75rem;
        color: var(--text-muted);
        font-family: monospace;
    }

    .code-content {
        padding: 16px;
        font-family: "Fira Code", "Menlo", monospace;
        font-size: 0.85rem;
        line-height: 1.6;
        overflow-x: auto;
        color: #e2e8f0;
    }

    /* Syntax Highlighting Colors */
    .code-comment {
        color: #64748b;
        font-style: italic;
    }
    .code-keyword {
        color: #c084fc;
    } /* Purple */
    .code-string {
        color: #86efac;
    } /* Green */

    /* 7. Footer Grid / Info Boxes */
    .footer-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 24px;
    }

    .warning-box,
    .tip-box {
        padding: 20px;
        border-radius: 12px;
        display: flex;
        gap: 16px;
        font-size: 0.95rem;
    }

    .warning-box {
        background: rgba(239, 68, 68, 0.1);
        border: 1px solid rgba(239, 68, 68, 0.2);
        color: #fca5a5;
    }

    .tip-box {
        background: rgba(59, 130, 246, 0.1);
        border: 1px solid rgba(59, 130, 246, 0.2);
        color: #93c5fd;
    }

    .warning-box ul,
    .tip-box ul {
        margin: 8px 0 0 20px;
        padding: 0;
    }

    .info-box {
        background: rgba(255, 255, 255, 0.05);
        padding: 12px;
        border-radius: 8px;
        font-size: 0.9rem;
        margin-bottom: 16px;
        color: var(--text-muted);
    }

    .split-content {
        display: grid;
        grid-template-columns: 1fr 1.5fr;
        gap: 32px;
        align-items: center;
    }

    .check-list li {
        margin-bottom: 8px;
        list-style-type: none;
        position: relative;
        padding-left: 24px;
        color: var(--text-muted);
    }

    .check-list li::before {
        content: "✓";
        position: absolute;
        left: 0;
        color: var(--accent-orange);
    }

    /* 8. Animations */
    @keyframes fadeIn {
        from {
            opacity: 0;
        }
        to {
            opacity: 1;
        }
    }

    @keyframes slideUp {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .animate-fade-in {
        animation: fadeIn 0.8s ease-out forwards;
    }

    .animate-slide-up {
        opacity: 0;
        animation: slideUp 0.6s ease-out forwards;
        animation-delay: var(--delay, 0ms);
    }

    /* 9. Responsive */
    @media (max-width: 768px) {
        .title {
            font-size: 2.5rem;
        }
        .footer-grid,
        .split-content {
            grid-template-columns: 1fr;
        }
        .card.full-width .terminal-window {
            margin-top: 24px;
        }
    }
</style>
